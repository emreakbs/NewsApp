@model ImageApp.Bussiness.Dto.ImageDto
@{
    ViewData["Title"] = "İçerik Detay";
}
<div class="col-xl-10 col-sm-12 mx-auto ">
    <h3 class="text-center mb-4">@Model.Title</h3>
    <h5 class="text-center">@Model.Description</h5>
    <div>
        @Html.Raw(@WebUtility.HtmlDecode(Model.Content))
    </div>

</div>